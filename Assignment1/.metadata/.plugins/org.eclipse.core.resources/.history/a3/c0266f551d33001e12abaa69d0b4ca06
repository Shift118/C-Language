#include <stdio.h>

int main(void){
	setbuf(stdout,NULL);
	float r;
	printf("Enter Circle Radius in meters: ");
	scanf("#f",&r);

	float area = (22/7)*r^2;
	float per = 2*(22/7)*r;
	printf("Radius: %f\nArea: %f\nPerimeter: %f.",r,area,per);
}
